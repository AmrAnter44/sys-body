# دليل التحديث التلقائي - Auto Update Guide

## ✅ تم التطبيق بنجاح!

تم إضافة نظام التحديث التلقائي بنجاح باستخدام **electron-updater**. الآن التطبيق يفحص التحديثات تلقائياً ويحملها ويثبتها.

---

## 🎯 كيف يعمل النظام؟

### 1. الفحص التلقائي
- عند فتح التطبيق، يفحص تلقائياً بعد 3 ثواني
- يفحص كل 10 دقائق تلقائياً
- في وضع Development، التحديث معطّل

### 2. عند وجود تحديث
1. ✅ يظهر تنبيه أزرق: "تحديث جديد متاح!"
2. 📥 يبدأ التحميل تلقائياً
3. 📊 يعرض نسبة التحميل
4. ✅ عند الانتهاء، يظهر تنبيه أخضر: "التحديث جاهز!"
5. 🔄 يعرض زر "إعادة تشغيل وتثبيت"

### 3. التثبيت
- المستخدم يضغط "إعادة تشغيل وتثبيت"
- التطبيق يُغلق ويُثبت التحديث
- يُفتح تلقائياً بالنسخة الجديدة

---

## 📦 نشر التحديثات

### الخطوات:

#### 1. زيادة رقم الإصدار في package.json
```json
{
  "version": "1.0.1"  // كان 1.0.0
}
```

#### 2. بناء التطبيق
```bash
npm run build
npm run electron:build:win
```

#### 3. رفع الملفات
ستجد الملفات في مجلد `dist/`:
- `X-Gym-Management-Setup-1.0.1.exe` - ملف التثبيت
- `latest.yml` - معلومات التحديث

#### 4. رفع الملفات على السيرفر
ضع الملفات في المسار:
```
http://your-server.com/updates/
├── X-Gym-Management-Setup-1.0.1.exe
└── latest.yml
```

#### 5. تحديث URL في package.json
```json
{
  "build": {
    "publish": {
      "provider": "generic",
      "url": "http://your-server.com/updates"
    }
  }
}
```

---

## 🔧 الإعدادات الحالية

### في main.js:
- ✅ `autoDownload: true` - تحميل تلقائي
- ✅ `autoInstallOnAppQuit: true` - تثبيت عند الإغلاق
- ✅ فحص كل 10 دقائق
- ✅ معطّل في Development

### في package.json:
```json
{
  "build": {
    "appId": "com.xgym.management",
    "productName": "X Gym Management",
    "publish": {
      "provider": "generic",
      "url": "http://localhost:3000/updates"
    }
  }
}
```

---

## 🌐 خيارات النشر

### 1. GitHub Releases (مجاني)
```json
{
  "publish": {
    "provider": "github",
    "owner": "your-username",
    "repo": "gym-management"
  }
}
```

**المميزات:**
- ✅ مجاني 100%
- ✅ سهل الاستخدام
- ✅ آمن
- ✅ تاريخ إصدارات واضح

**الخطوات:**
1. أنشئ repository على GitHub (يمكن يكون private)
2. غيّر الإعدادات في package.json
3. شغّل: `npm run electron:build:win`
4. ارفع الملفات على GitHub Releases

### 2. سيرفرك الخاص (أفضل للخصوصية)
```json
{
  "publish": {
    "provider": "generic",
    "url": "https://your-domain.com/updates"
  }
}
```

**المميزات:**
- ✅ تحكم كامل
- ✅ خصوصية 100%
- ✅ سرعة أعلى للفروع المحلية
- ❌ يحتاج سيرفر

**الخطوات:**
1. أعد سيرفر (Apache/Nginx/IIS)
2. ضع الملفات في مجلد public
3. تأكد من:
   - الملفات accessible عبر HTTP/HTTPS
   - CORS مفعّل (إذا لزم الأمر)
   - SSL certificate (للأمان)

### 3. AWS S3 / DigitalOcean Spaces
```json
{
  "publish": {
    "provider": "s3",
    "bucket": "gym-management-updates",
    "region": "us-east-1"
  }
}
```

---

## 🧪 اختبار التحديث

### في Development:
التحديث معطّل تلقائياً. للاختبار:

1. **Build التطبيق:**
   ```bash
   npm run build
   npm run electron:build:win
   ```

2. **ثبّت النسخة 1.0.0:**
   - ثبّت من `dist/X-Gym-Management-Setup-1.0.0.exe`

3. **غيّر version في package.json إلى 1.0.1**

4. **Build مرة ثانية:**
   ```bash
   npm run electron:build:win
   ```

5. **ضع ملفات 1.0.1 على السيرفر**

6. **افتح التطبيق المثبت (1.0.0)**
   - يجب أن يفحص ويجد 1.0.1
   - يحمله تلقائياً
   - يعرض تنبيه "التحديث جاهز!"

---

## 🎨 واجهة المستخدم

### التنبيهات:

#### 1. تحميل التحديث (أزرق)
```
🔄 تحديث جديد متاح!
النسخة 1.0.1

[████████░░] 80%

جاري تحميل التحديث...

                    [✕]
```

#### 2. جاهز للتثبيت (أخضر)
```
✅ التحديث جاهز!
النسخة 1.0.1 جاهزة للتثبيت

[إعادة تشغيل وتثبيت]  [لاحقاً]
```

#### 3. خطأ (أحمر)
```
❌ خطأ في التحديث
Failed to fetch update

                    [✕]
```

---

## 🔍 Troubleshooting

### المشكلة: لا يفحص التحديثات
**الحل:**
- تأكد أنك في Production mode (ليس Development)
- تحقق من console logs
- تأكد من الاتصال بالإنترنت

### المشكلة: "Update not available"
**الحل:**
- تأكد أن version في package.json أكبر
- تأكد من رفع latest.yml على السيرفر
- تحقق من URL في publish settings

### المشكلة: خطأ في التحميل
**الحل:**
- تأكد من رفع .exe على نفس مسار latest.yml
- تحقق من permissions على السيرفر
- تأكد من حجم الملف صحيح

---

## 📝 ملاحظات مهمة

1. ✅ **Code Signing** (اختياري لكن مُنصح):
   - يمنع تحذيرات Windows
   - يثبت أنك الناشر الرسمي
   - يحتاج certificate من Sectigo أو DigiCert

2. ✅ **Testing**:
   - اختبر التحديث دائماً قبل نشره للجميع
   - جرّب على نسخة قديمة أولاً
   - تأكد من حجم الملف معقول

3. ✅ **Versioning**:
   - استخدم Semantic Versioning (1.0.0, 1.0.1, 1.1.0, 2.0.0)
   - Major.Minor.Patch
   - زد Patch للإصلاحات الصغيرة
   - زد Minor للميزات الجديدة
   - زد Major للتغييرات الكبيرة

4. ✅ **Rollback**:
   - احتفظ بنسخ قديمة احتياطياً
   - إذا فيه مشكلة، ارجع للنسخة القديمة على السيرفر

---

## 🚀 الخطوات التالية

### الآن:
1. ✅ غيّر URL في package.json لسيرفرك
2. ✅ جرّب التحديث محلياً
3. ✅ وزّع على فرع واحد للاختبار

### قريباً:
1. 🔒 أضف Code Signing
2. 📊 أضف Analytics لتتبع التحديثات
3. 🔔 أضف Release Notes في التنبيه

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. تحقق من console logs في Electron
2. تحقق من ملف latest.yml
3. تأكد من الـ URL صحيح

**الملفات المهمة:**
- `electron/main.js` - إعداد autoUpdater
- `electron/preload.js` - IPC bridge
- `components/UpdateNotification.tsx` - واجهة المستخدم
- `package.json` - إعدادات النشر

---

## ✅ ملخص

✨ **تم إضافة:**
- ✅ فحص تلقائي للتحديثات كل 10 دقائق
- ✅ تحميل تلقائي في الخلفية
- ✅ تنبيهات جميلة بالعربي والإنجليزي
- ✅ شريط تقدم التحميل
- ✅ زر إعادة تشغيل وتثبيت
- ✅ معالجة الأخطاء

🎯 **النتيجة:**
الآن يمكنك نشر تحديثات لجميع الفروع بنقرة واحدة، وكل الأجهزة تتحدث تلقائياً بدون تدخل يدوي!
